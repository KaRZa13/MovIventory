image: node:18

stages:
  - install
  - build

cache:
  paths:
    - node_modules/

variables:
  EXPO_TOKEN: $EXPO_TOKEN
  CI: "true"

before_script:
  - npm ci
  - npm install -g eas-cli expo-cli

install:
  stage: install
  script:
    - echo "✅ Installation terminée"

build_android:
  stage: build
  script:
    - echo "🔐 Authentification Expo..."
    - eas whoami || eas login --token $EXPO_TOKEN
    - echo "🏗️  Build APK en cours..."
    - eas build --platform android --profile preview --non-interactive
  only:
    - main
